[project]
name = "dynamic-waste-agent"
version = "0.1.0"
description = "LangGraph multi-agent orchestration layer for dynamic construction waste handling."
requires-python = ">=3.10"
dependencies = [
  "langgraph>=1.2.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
