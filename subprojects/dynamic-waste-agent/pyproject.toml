[project]
name = "dynamic-waste-agent"
version = "0.1.0"
description = "Placeholder multi-agent orchestration layer for dynamic construction waste handling."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
