# dynamic-waste-ui 开发约束

本目录只负责用户界面和可视化。

## 设计原则

- 优先做工作台式界面：信息密集、状态清晰、操作可确认。
- 高风险机器人动作必须有明确确认状态。
- 不使用装饰性过强的落地页作为主界面。

## 工程边界

- 不嵌入模型训练逻辑。
- 不把实验论文文档放入前端目录。
- API 契约应与 `dynamic-waste-agent` 或后端服务对齐。
